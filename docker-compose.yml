version: "3.9"

services:
  server:
    build: ./server
    container_name: express-server

    ports:
      - "5000:5000"

    restart: always

  client:
    build: ./client
    container_name: react-client

    ports:
      - "5173:5173"

    depends_on:
      - server

    restart: always
